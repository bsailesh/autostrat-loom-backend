fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
anthropic>=1.1.0
bcrypt>=4.1,<5
python-dotenv==1.0.1
python-multipart==0.0.9
psycopg2-binary==2.9.9
pytest==8.3.3
# Used by fastapi/starlette TestClient. Was previously pulled in transitively by
# the old Anthropic SDK (0.39); the 1.x SDK moved to httpx2, so it's explicit now.
httpx==0.27.2
